# golang-todo-cli

for add task:
``` bash
go run ./todo -add <your task>
```
for delete task:
``` bash
go run ./todo -del=<task-id>
```
for complete task:
``` bash
go run ./todo -complete=<task-id>
```
for show all tasks:
``` bash
go run ./todo -list
```